{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "python -m uvicorn src.main:app --host :: --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
